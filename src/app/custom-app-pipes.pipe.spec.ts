import { CustomAppPipesPipe } from './custom-app-pipes.pipe';

describe('CustomAppPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomAppPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
